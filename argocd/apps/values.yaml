# =============================================================================
# Application Deployments - ApplicationSet Configuration
# =============================================================================
# This ApplicationSet automatically discovers and deploys all Helm charts
# found in the apps/ directory. No manual registration required.
#
# To add a new application:
#   1. Create a Helm chart in apps/<name>/
#   2. The ApplicationSet will automatically detect and deploy it
# =============================================================================

repoURL: https://github.com/markis/cluster-config.git
targetRevision: main
basePath: apps
