apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Chart.Name }}-config"
  labels:
    app: {{ .Chart.Name }}
data:
  settings.json: |
    {{- tpl (.Files.Get "config/settings.json") . | nindent 4 }}
